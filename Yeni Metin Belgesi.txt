


1.NPM (Node Package Manager), Javascript dilleri için paket yöneticisidir. Kullanýcýlar tarafýndan oluþturulan veya topluluk tarafýndan sunulan paketleri indirmek, yüklemek ve kullanmak için kullanýlýr.

2.Async - Await, Javascript'te asenkron iþlemleri yapmak için kullanýlan bir yapýdýr. Kodlar arasýnda beklemeler oluþmasýný önler ve daha okunabilir kodlar oluþmasýný saðlar. Genellikle Node.js veya web uygulamalarýnda kullanýlýr.

3.Express, Node.js tabanlý web uygulamalarý yazmak için bir kütüphanedir. HTTP istek ve yanýtlarýný yönetmek, rotalama yapmak, middleware yapýlarýný kullanmak gibi iþlemleri kolaylaþtýrýr.

4.Javascript'te fibonacci sayýlarýný hesaplayan bir program;
   
function fibonacci(num) {
  if (num <= 1) return num;
  return fibonacci(num - 1) + fibonacci(num - 2);
}

Recursive (Özyinelemeli), kendisini tekrar tekrar çaðýrarak iþlem yapmaya olanak tanýyan fonksiyonlar anlamýna gelir.


5. JSON (JavaScript Object Notation), verilerin iletiminde kullanýlan bir veri formatýdýr. XML'e göre daha hafif ve okunabilirdir. XML ise, verilerin düzenli bir þekilde iletimini saðlamaya yönelik bir veri formatýdýr ve JSON'a göre daha esnektir.

6.Module Export, Javascript dosyalarý arasýnda veri ve fonksiyonlarýn paylaþýmýný saðlamak için kullanýlan bir yapýdýr. Dosyalar arasýnda module.exports veya exports kullanýlarak veri veya fonksiyonlar tanýmlanýr ve baþka dosyalarda require fonksiyonu ile kullanýlabilir.

7.Javascript, web tarafýnda çalýþan, dinamik bir programlama dilidir. Typescript ise, Javascript'e tip tanýmlama özelliði ekleyen bir dil dilidir ve daha güvenli ve büyük projelerde kolaylaþtýrýcý özelliklere sahiptir.

8.Axios, JavaScript dilleri için HTTP isteklerini yapmak için kullanýlan bir araçtýr. Axios ile REST API'lere veya diðer HTTP kaynaklarýna istek yapabilir, verileri alabilir ve yanýtlarý iþleyebilirsiniz. Axios, Promise tabanlý ve birçok platformda kullanýlabilir, ayný zamanda HTTP taleplerinde otomatik olarak JSON verilerini dönüþtürür.

9.GET methodu, sunucudan veri almak için kullanýlýr. GET isteði URL'ye eklenen verilerle birlikte gönderilir ve sunucudan dönen veriler tarayýcýnýn geçici olarak sakladýðý verilerdir. GET istekleri güvenli deðildir ve sunucuya veri gönderme iþlemi yapýlmaz.

POST methodu ise, sunucuya veri göndermek için kullanýlýr. POST isteði, verilerin sunucuya gönderildiði bir form olarak gönderilir ve sunucuya veri gönderme iþlemi yapýlabilir. POST istekleri güvenli bir yöntemdir ve sunucuya daha büyük veri miktarlarýný göndermeye olanak tanýr.

Kýsaca, GET isteði sunucudan veri almak için, POST isteði ise sunucuya veri göndermek için kullanýlýr.
